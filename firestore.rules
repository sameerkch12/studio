
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow public read/write access to all collections for this simple app.
    // In a real-world scenario, you would implement proper security rules.
    match /{collection}/{document=**} {
      allow read, write: if true;
    }
  }
}
